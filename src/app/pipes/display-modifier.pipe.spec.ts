import { DisplayModifierPipe } from './display-modifier.pipe';

describe('DisplayModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
