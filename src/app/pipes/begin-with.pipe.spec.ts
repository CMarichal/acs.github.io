import { BeginWithPipe } from './begin-with.pipe';

describe('BeginWithPipe', () => {
  it('create an instance', () => {
    const pipe = new BeginWithPipe();
    expect(pipe).toBeTruthy();
  });
});
